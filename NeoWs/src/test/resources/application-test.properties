# Test Configuration
# Mock NASA API key for testing
nasa.api.key=test-api-key

# Kafka configuration for testing (will use mock beans)
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.template.default-topic=test-asteroid-alert

# Logging for tests
logging.level.com.onion.NeoWs=DEBUG
logging.level.org.springframework.kafka=OFF