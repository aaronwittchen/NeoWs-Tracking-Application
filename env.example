# NeoWs Tracking Application - Environment Configuration Template
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env, Run: cp .env.example .env
# 2. Fill in your actual values in the .env file
# This template covers all services in the NeoWs tracking application:
# - NeoWs Service (asteroid data processing)
# - Email Notification Service (user alerts)
# - MySQL Database
# - Kafka Message Broker

# NASA API Configuration
# Get your free API key from: https://api.nasa.gov/
# This key is used to fetch asteroid data from NASA's Near-Earth Object API
NASA_API_KEY=DEMO_KEY

# Database Configuration
# MySQL database credentials for the asteroid alert database
# Make sure the database 'asteroidalert' exists before starting the services
DB_USERNAME=root
DB_PASSWORD=password

# Email Service Configuration (Gmail SMTP)
# Get your Gmail App Password from: https://myaccount.google.com/apppasswords
# Note: You must enable 2-factor authentication to use app passwords
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=your_email@gmail.com
SPRING_MAIL_PASSWORD=your_gmail_app_password
MAIL_FROM_EMAIL=your_email@gmail.com

# Testing Configuration (MailTrap)
# Get a free account at https://mailtrap.io/ for safe email testing
# Emails sent during development will be captured here instead of real inboxes
MAILTRAP_USERNAME=your_mailtrap_username
MAILTRAP_PASSWORD=your_mailtrap_password

# Kafka Configuration
# Kafka broker addresses (comma-separated for multiple brokers)
# For local development with Docker: localhost:9092
# For production: use your actual Kafka cluster addresses
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Kafka topic for asteroid alerts (will be auto-created if it doesn't exist)
KAFKA_TOPIC_ASTEROID_ALERT=asteroid-alert

# Consumer group ID for the email notification service
KAFKA_CONSUMER_GROUP_ID=asteroid-alert-consumers

# Service Ports
# Uncomment and modify these if you need different ports

# NeoWs service port (default: 8080)
# NEO_WS_SERVER_PORT=8080

# Email notification service port (default: 8082)
# EMAIL_SERVICE_SERVER_PORT=8082

# Logging Configuration
# Uncomment and modify these to change logging levels

# Root logging level (options: ERROR, WARN, INFO, DEBUG, TRACE)
# LOGGING_LEVEL_ROOT=INFO

# Application logging levels
# LOGGING_LEVEL_COM_ONION_NEO_WS=INFO
# LOGGING_LEVEL_COM_ONION_EMAILNOTIFICATIONSERVICE=INFO

# Kafka logging level (reduce noise from Kafka internals)
# LOGGING_LEVEL_ORG_APACHE_KAFKA=WARN

# Environment Mode
# Set to 'prod' for production deployment
# ENVIRONMENT=dev